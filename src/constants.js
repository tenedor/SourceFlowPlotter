(function(){

_.extend(sfp, {

  // for now, data is hardcoded to a demo script called `test.py`
  // TODO: generalize beyond single hardcoded file
  DATA_URL: 'data/test.py.trace.json',
  FILE_NAME: 'test.py'

});

}).call(this);
